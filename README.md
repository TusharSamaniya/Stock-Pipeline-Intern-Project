# Stock Market Data Pipeline with Airflow

A Dockerized end-to-end data pipeline that fetches real-time stock data from an API, parses and cleans the JSON response, and stores the processed records into a PostgreSQL database on a scheduled basis using Apache Airflow.
